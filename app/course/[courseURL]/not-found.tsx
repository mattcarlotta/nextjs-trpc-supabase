import CourseNotFound from "./CourseNotFound";

export default function CourseNotFoundPage() {
    return <CourseNotFound error="We're unable to locate that course. It either doesn't exist or was removed." />;
}
